const da = new Date();
/* Setting the value of the input field with the id of `load-date-time` to the value of the variable
`da` which is a date object. */
document.querySelector("#load-date-time").value = da